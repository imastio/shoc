const availableLocales = ["en"];
const defaultLocale = availableLocales[0];

const localeLabels = [
    {
        locale: "en",
        text: "English"
    }
]

const localeConfig = { 
    availableLocales, 
    defaultLocale, 
    localeLabels
};

export default localeConfig;